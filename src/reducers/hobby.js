const hobbyReducer = (state, action) => {
    switch (action.type) {
        case "ADD_HOBBY":

            break;
        case "SET_ACTION_HOBBY":

            break;

        default:
            return state;
    }
}

export default hobbyReducer